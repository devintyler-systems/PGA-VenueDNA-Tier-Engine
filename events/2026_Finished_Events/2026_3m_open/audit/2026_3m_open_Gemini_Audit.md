Target IdentificationTarget files requiring immediate modification by Claude Code:app.js (State tracking, modal join logic, UI conditionals)engine/enrich_cards.py (LLM narrative generation pipeline)engine/build_round_analysis.py (Round analysis compilation)index.html (Empty state rendering, tab labeling)Context & Technical ApproachThe live round modal is silently failing data joins and reverting 90% of the field to a 50.0 VTS placeholder. The narrative layer is successfully joining text, confirming a decoupled architecture where narrative text and trait probability data traverse separate pipelines. The text pipeline itself is suffering from severe prompt collapse, recycling a single approach-SG risk vector across 11 of 12 top contenders and mirroring it across distinct UI slots within individual player cards.  The architectural fix decouples the narrative generation from static templating. The system must enforce a structured JSON schema in the LLM generation loop to isolate the "Win Mechanism", "Key Risk", and "Skill Read" variables. The frontend modal requires a strict index match using norm_name between the leaderboard_snapshot in rN_analysis.json and the Pre-Tournament VTS payload. To support multi-event learning, a persistent SQLite database will replace flat JSONs for tracking weight calibration and Spearman rho delta over time.  Deterministic Requirements1. Data Join Hardening & Modal IndexingModify app.js round-modal logic. When a player is clicked from any live leaderboard, execute a strict string match using the norm_name key from the leaderboard_snapshot against the Pre-Tournament VTS dictionary.  Clamp the 50.0 VTS "Alternate Entry" template to only trigger if the pt_flags attribute explicitly contains "Late Alternate".  If a join fails and the player is not an alternate, throw a hard console error and halt rendering rather than masking the failure with baseline data.  2. Narrative Schema EnforcementUpdate engine/enrich_cards.py to enforce structured JSON output from the LLM.Create a Pydantic model or exact JSON schema defining three distinct string fields: win_mechanism, key_risk, and skill_read.Inject a prompt constraint mandating mutually exclusive data points across the three fields, drawing directly from the trait_audit delta values.  Generate a dynamic daily brief utilizing the live rank_delta and live_lean_notes vectors to replace static pre-tournament text.  3. UI Component Alignment & Data Sparsity HandlingEvaluate metadata.is_final from rN_analysis.json to toggle the round sub-tab labels from "R1 Live" to "R1 Final".  Construct a universal EmptyState component in app.js and apply it to the Tee-Times tab and the Final tab to intercept the HTTP 404 error.  Read the course history array length for the "VTS Journey" chart. If the array contains fewer than 3 signal points, suppress the chart entirely and render a "Insufficient Course History" badge.  4. Multi-Horizon Learning Ledger InitializationCreate engine/init_ledger.py utilizing Python sqlite3.Define a table model_calibration tracking event_slug, round, spearman_rho, and the 10 trait weights (e.g., app_wedge).  Update engine/build_round_analysis.py to append the final model_performance.spearman_rho value to this SQLite table at the conclusion of each round execution.  Verification Loop CommandsClaude Code must execute the following terminal commands to confirm compilation and data integrity:Bashpython engine/build_round_analysis.py --round 1
python engine/build_round_analysis.py --round 2
python engine/enrich_cards.py --force-refresh
python -c "import json; data = json.load(open('output/round1/r1_analysis.json')); print('Schema Valid' if data['schema_version'] == '1.1' else 'Schema Drift')"
python -c "import sqlite3; db = sqlite3.connect('venue_dna_ledger.db'); print(db.execute('SELECT COUNT(*) FROM model_ca