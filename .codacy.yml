---
exclude-paths:
  - node_modules
  - dist
  - build
  - vendor
  - .venv
  - venv
exclude-patterns:
  - ^(.*/)?.*\.min\.(js|css)$
  - ^(.*/)?.*(test|spec)\.(py|js|ts)$
engines:
  eslint:
    enabled: true
  pmd:
    enabled: true
  remark-lint:
    enabled: true
  metrics:
    enabled: true